package main

import (
  "fmt"
  "strconv"
)

func main() {
  var (
    s string
    ans int
  )
  fmt.Scan(&s)
  s := make([][]string, h)
  	for i:=0; i<h; i++ {
  		fmt.Scan(&tmp)
  		for _, c := range tmp {
  			s[i] = append(s[i], string(c))
  		}
  	}
  fmt.Println(ans)
}
